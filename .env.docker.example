# Copy to ".env" next to docker-compose.yml (or set these in the Portainer stack UI).
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o
# Host port the reverse proxy listens on (browse http://<host>:8080).
PROXY_PORT=8080
